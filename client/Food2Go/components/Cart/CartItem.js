import { View, Text } from 'react-native'
import React from 'react'

const CartItem = ({ dish, handleDeleteCart, cart }) => {
  return (
    <View>
      <Text>CartItem</Text>
    </View>
  )
}

export default CartItem